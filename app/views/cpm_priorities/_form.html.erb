<%= form_for(@cpm_priority) do |f| %>
  <% if @cpm_priority.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cpm_priority.errors.count, "error") %> prohibited this cpm_priority from being saved:</h2>

      <ul>
      <% @cpm_priority.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.text_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
