<%= form_for(@cpm_team) do |f| %>
  <% if @cpm_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cpm_team.errors.count, "error") %> prohibited this cpm_team from being saved:</h2>

      <ul>
      <% @cpm_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
